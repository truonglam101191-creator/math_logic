import 'package:flutter_test/flutter_test.dart';
import 'package:oziapi/models/device_info_model.dart';
import 'package:oziapi/requests/request.dart';

void main() {
  test('is the default instance', () async {
    // final request = Request();
    // final deviceInfo = DeviceInfoModel(
    //   model: 'sdk_gphone64_x86_64',
    //   manufacturer: 'Google',
    //   brand: 'google',
    //   os: 'Android',
    //   version: '13',
    //   isPhysicalDevice: false,
    //   id: 'FE1A.220922.010',
    // );
    // final a = await request.loginguest(deviceInfo: deviceInfo);
    // expect(a!.errorCode, 200);
  });
}
